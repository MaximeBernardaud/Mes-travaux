{% extends 'base.html.twig' %}

{% block content %}
    <h1>Inscription</h1>
    {% if notification %}
        <div class="aler alert-info">{{ notification }}</div>
        <hr>
    {% endif %}
    {{ form(form) }}

{% endblock %}